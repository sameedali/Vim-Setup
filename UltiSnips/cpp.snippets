snippet forj "forj loop (forj)"
for (int ${2:j} = 0; $2 < ${1:count}; ${3:++$2}) {
	${VISUAL}${0}
}
endsnippet


snippet fork "fork loop (fork)"
for (int ${2:k} = 0; $2 < ${1:count}; ${3:++$2}) {
	${VISUAL}${0}
}
endsnippet
